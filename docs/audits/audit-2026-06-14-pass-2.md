# TimeTrack — audit pass 2 (2026-06-14)

**Branch:** `fix/audit-pass-2-2026-06-14`
**Builds on:** `fix/audit-pass-2026-06-14` (pass 1)

## What landed in this pass

| # | Fix | File(s) |
|---|---|---|
| 1 | Enable `esModuleInterop` in tsconfig (silences ts-jest advisory, simpler default imports) | `tsconfig.json` |
| 2 | Remove `limit(500)` from `getAllTimeEntries` — silently truncated payroll. Now paginates with `startAfter` until exhausted. | `src/app/lib/database.ts` |
| 3 | TZ fix in `checkWrongDay` — UTC-anchored day-of-week instead of runtime local | `src/utils/scheduleHelpers.js` |
| 4 | TZ fix in `checkTimeAnomalies` weekend check | `src/utils/timeValidation.ts` |
| 5 | `calculateEstimatedHours` Invalid Date for non-UTC users — replaced Date math with minute arithmetic | `src/app/components/employee/TodayEntry.tsx` |
| 6 | Entry-access UX: when blocked, hide the form fields entirely (was: only disabled submit, still showed editable inputs next to red block banner) | `src/app/components/employee/TodayEntry.tsx` |
| 7 | Login error UX: persistent inline `<Alert>` instead of ephemeral Sonner toast (a11y finding from 2026-04-17 report) | `src/app/components/LoginPage.tsx` |
| 8 | Regression test for `checkWrongDay` TZ behaviour | `src/utils/scheduleHelpers.tz.test.ts` (new) |

## Scores

- Jest: **146/146** across 8 suites (was 140)
- tsc: clean
- build: clean (1751 modules, 1.46s)

## Still open (deferred)

- Wire `ClockPunch` as primary UI and retire `TodayEntry` (still renders both stacked)
- CI hook (`npm test && npm run build` on pre-merge)
- Re-run `npm run test:rules` (Firestore rules)
- Investigate whether `TodayEntry`'s `Start New Shift` button is double-click safe (the `submitting` guard is there but the audit flagged it for a reason)
- Add Playwright smoke against the seeded test user (per the 2026-04-17 plan)

## Deploy

Same as pass 1 — local Firebase auth not configured, branch pushed for review/merge. Promote with `firebase deploy --only hosting` once merged to main.
