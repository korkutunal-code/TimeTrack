# TimeTrack — audit pass 3 (2026-06-14)

**Branch:** `fix/audit-pass-3-2026-06-14`
**Builds on:** `fix/audit-pass-2-2026-06-14` (pass 2)

## What landed in this pass

| # | Fix | File(s) |
|---|---|---|
| 1 | `ClockPunch` is now the only employee "today" view. `TodayEntry` is reachable only via `?classic=1` URL flag for fallback. | `src/app/App.tsx` |
| 2 | GitHub Actions CI: runs `npm test`, `tsc --noEmit`, `npm run build` on every push to main and every PR. | `.github/workflows/ci.yml` (new) |
| 3 | Firestore rules test re-attempted; emulator not reachable in this environment so couldn't run. Documented. | n/a |

## Scores

- Jest: **146/146** across 8 suites
- tsc: clean
- build: clean

## Still open

- `npm run test:rules` against the live emulator (needs `firebase emulators:start`)
- Playwright smoke against a seeded test user
- `TodayEntry` "Start New Shift" double-click race investigation
- Grep audit for any remaining `getDay()` / `getHours()` calls using local-TZ parsing
- Wire the `clockService` transactions into the admin correction flow (currently admin edits bypass the segment model)

## Deploy

Same as previous passes — branch pushed for review/merge. CI will run automatically on PR open.
