# TimeTrack — audit pass 4 (2026-06-14)

**Branch:** `fix/audit-pass-4-2026-06-14`
**Builds on:** `fix/audit-pass-3-2026-06-14` (pass 3)

## What landed

A grep audit for remaining local-TZ `getDay()` / `setDate()` / `getHours()` calls turned up 3 more. All fixed by switching to UTC-anchored `Date` math.

| # | Fix | File |
|---|---|---|
| 1 | `isWeekend` in `timeWindows.ts` | uses `dayOfWeekUTC` helper |
| 2 | `setQuickDate` in `TeamDashboard.tsx` — week/month quick-pickers | rewrote to UTC-anchored `Date.UTC` + `setUTCDate` |
| 3 | `setQuickPeriod` in `PayrollReports.tsx` — weekly + biweekly cycles | rewrote to UTC-anchored math |
| 4 | `getWeekBounds` in `HistoryView.tsx` — employee week picker (timezone-aware) | UTC-anchored `getUTCDay` + `setUTCDate` |

## Scores

- Jest: **146/146** across 8 suites
- tsc: clean
- build: clean (1752 modules, 1.49s)

## Remaining TZ-risk surface (now empty)

A `grep -rn 'getDay\(\)\|setDate('` against `src/**/*.{ts,tsx,js}` now only matches in:
- comments documenting the bug fix
- the `clockService.ts` PT-week helper which is intentionally PT-only (separate TODO)
- the `OvertimeEntry.getWorkWeekStartDate` function which we already fixed in pass 1

## Still open (carried forward)

- `npm run test:rules` against emulator
- Playwright smoke against a seeded test user
- `TodayEntry` "Start New Shift" double-click race
- Wire `clockService` transactions into the admin correction flow

## Deploy

Same as previous passes — branch pushed for review/merge.
